'use client';
import React from 'react';

const AddToCart = () => {
  return (
    <div>
      <button onClick={() => alert('clicked')}>add to cart</button>
    </div>
  );
};

export default AddToCart;
